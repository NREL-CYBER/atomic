import { IonText } from '@ionic/react';
import React from 'react';

/**
 * Component to display text with optional color
 */
const AppText = props => {
  return /*#__PURE__*/React.createElement(IonText, props);
};

export default AppText;