import { IonIcon } from '@ionic/react';
import React from 'react';

/**
 * Component to display an icon
 */
const AppIcon = props => /*#__PURE__*/React.createElement(IonIcon, props);

export default AppIcon;