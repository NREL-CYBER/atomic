import { AppColor } from "atomic";
import React from "react";
export declare const AppFormLabel: React.FC<{
    name: string;
    onClick?: () => void;
    color: AppColor;
    info?: string;
    required?: boolean;
}>;
