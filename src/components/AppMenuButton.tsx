import { IonMenuToggle, IonMenuButton } from '@ionic/react';
import React from 'react';



/**
 * Component for a button on a menu
 */
const AppMenuButton: React.FC = (props) =>
    <IonMenuButton {...props} />

export default AppMenuButton;
